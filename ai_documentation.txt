git commit -m 'add ed ligotools folder' 
improve my commit message

-> chatGPT suggested the message 'moved readligo.py into ligotools folder'



--------------------------------------------------------------------------- AttributeError Traceback (most recent call last) Cell In[16], line 18 16 # to remove effects at the beginning and end of the data stretch, window the data 17 # https://en.wikipedia.org/wiki/Window_function#Tukey_window ---> 18 try: dwindow = signal.tukey(template.size, alpha=1./8) # Tukey window preferred, but requires recent scipy version 19 except: dwindow = signal.blackman(template.size) # Blackman window OK if Tukey is not available AttributeError: module 'scipy.signal' has no attribute 'tukey' During handling of the above exception, another exception occurred: AttributeError Traceback (most recent call last) Cell In[16], line 19 16 # to remove effects at the beginning and end of the data stretch, window the data 17 # https://en.wikipedia.org/wiki/Window_function#Tukey_window 18 try: dwindow = signal.tukey(template.size, alpha=1./8) # Tukey window preferred, but requires recent scipy version ---> 19 except: dwindow = signal.blackman(template.size) # Blackman window OK if Tukey is not available 21 # prepare the template fft. 22 template_fft = np.fft.fft(template*dwindow) / fs AttributeError: module 'scipy.signal' has no attribute 'blackman'

How can I fix this

-> def tukey_window(N, alpha=0.5):
    return signal.windows.tukey(N, alpha)